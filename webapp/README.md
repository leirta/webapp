# 年表記帳本

這是一個純前端的個人記帳 Web App 原型。

## 目前功能

- 年度檢視直接編輯每月收入、帳戶餘額、固定支出
- 每日支出自動回填到年度檢視的支出分類
- 依月份查看收支摘要與支出清單
- 資料存於瀏覽器 `localStorage`

## 使用方式

直接用瀏覽器開啟 `index.html` 就可以開始使用。

如果要在同一個 Wi-Fi 下用手機開，可以之後再加一個簡單的本機靜態伺服器。
